# Base URL of the Express API. Defaults to same-origin /hcgi/api for local dev.
# Set to the full API origin when the API is hosted separately from the frontend,
# e.g. VITE_API_URL=https://api.example.com/hcgi/api
VITE_API_URL=
